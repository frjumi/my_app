.form_select
  = form_tag(display_theme_path, remote: true, format: :js) do
    = select_tag :theme,
        options_for_select(@themes),
        prompt: t('work.index.select_theme_prompt'),
        class: 'form-control theme-select-dropdown',
        onchange: "$(this).closest('form').submit();"
