.center.jumbotron
  %h1= t('.welcome')
  - if signed_in?
    = link_to t('.proceed'), work_path, class: "btn btn-primary"
  - else
    = link_to t('.signup_now'), signup_path, class: "btn btn-primary"